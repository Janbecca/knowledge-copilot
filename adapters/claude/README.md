# Claude adapter

Claude Desktop and Claude Code document local MCP server configuration; Claude.ai and the Anthropic API have different remote-connector constraints. Replace `ABSOLUTE_PATH` in the example before use.

MCP Apps UI support must be capability-negotiated. Tool-only operation remains valid if the selected Claude surface does not advertise the UI extension. This repository has not been installed into a live Claude product.
