import { describe,expect,it } from "vitest";
import { knowledgeCardSchema } from "../../packages/card-protocol/index.js";
import { service } from "../helpers.js";
describe("v2 card protocol",()=>{it("keeps the complete ESP32 operation model and filters progress noise",async()=>{const {store,service:s}=service();const session=s.start({extraction_mode:"server_llm"});await s.capture({session_id:session.session_id,user_message:"esptool read-flash 0 ALL backup.bin",assistant_message:"只读完成，请验证文件",tool_observations:["Reading 10%","Reading 20%"]});const cards=s.store.listCards(session.session_id);expect(cards).toHaveLength(1);expect(knowledgeCardSchema.parse(cards[0]).operation).toMatchObject({mode:"read_only",reversibility:"not_applicable"});expect(cards[0]!.operation?.verification.length).toBeGreaterThan(0);store.close();});});
