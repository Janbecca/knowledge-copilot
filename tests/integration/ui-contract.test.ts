import { readFileSync } from "node:fs";import { describe,expect,it } from "vitest";
describe("panel contract",()=>{it("visually distinguishes lifecycle events and exposes required controls",()=>{const css=readFileSync("apps/knowledge-panel/src/style.css","utf8");const ui=readFileSync("apps/knowledge-panel/src/main.ts","utf8");expect(css).toContain(".card.superseded");expect(css).toContain(".card.discarded");expect(ui).toContain("已修订");expect(ui).toContain("只看新增");expect(ui).toContain("Mermaid 思维导图");expect(ui).toContain('call("list_knowledge_cards"');expect(ui).toMatch(/since_cursor:\s*since/);expect(ui).toContain("当前 AI 直接整理");expect(ui).toContain("服务器 LLM");expect(ui).toContain("change_extraction_mode");});});
