# 贡献指南

欢迎贡献。请先在 Issue 中说明问题或提议，避免重复实现。

## 开发流程

1. 从 `main` 创建简短的功能或修复分支。
2. 保持改动聚焦；协议、数据库迁移和用户界面变更应附带相应测试。
3. 在提交 PR 前运行：

```powershell
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
npm.cmd run test:mcp
```

4. PR 描述应包括：问题、实现方式、验证结果、数据/隐私影响及兼容性影响。

## 知识提取质量反馈

请不要直接提交真实敏感对话、API Key、访问令牌、个人信息或 SQLite 数据库文件。提交最小化、脱敏的输入和预期卡片/导出结果；维护者会将可复现案例转为测试夹具或评测样例。

## 兼容性与版本

任何变更卡片协议、SQLite schema 或 MCP 工具输入输出的 PR，必须说明迁移和向后兼容策略。重大不兼容变更只在新的主版本中发布。
