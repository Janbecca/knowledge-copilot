# 安全策略

## 支持范围

当前支持线为最新 `main` 和最新发布版本。Beta 版本仅适用于本地、已授权的数据处理场景。

## 报告漏洞

公开 GitHub 仓库建立后，请通过 GitHub Security Advisory 私密报告安全问题，不要在公开 Issue 中披露可利用细节、密钥或用户数据。

报告请包含复现步骤、受影响版本、潜在影响和可选修复建议。维护者会确认接收、评估影响并在修复发布后提供公告。

## 数据处理边界

项目默认使用本地 SQLite；启用 LLM 提取器时，会将配置范围内的对话和卡片上下文发送至用户选择的兼容端点。请勿提交密钥、数据库或未脱敏的用户对话作为公开问题材料。
